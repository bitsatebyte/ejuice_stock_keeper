import React from 'react';
import ReactDOM from 'react-dom';
import App from './App.js';

// Renders the app component and injects it into the root element
ReactDOM.render(<App />, document.getElementById('root'));
